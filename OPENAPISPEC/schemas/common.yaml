components:
  schemas:
    GuidValueResult:
      title: GuidValueResult
      type: object
      properties:
        id:
          type: string
          format: uuid
      additionalProperties: false

    CustomError:
      title: CustomError
      type: object
      properties:
        message:
          type: string
          nullable: false
      additionalProperties: false
