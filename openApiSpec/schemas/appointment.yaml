components:
  schemas:    
    Appointment:
      title: Appointment
      type: object
      properties:
        appointmentData:
          type: object
          properties:
            startTime:
              type: string
              format: date-time
            endTime:
              type: string
              format: date-time
            title:
              type: string
              nullable: false
          additionalProperties: false
        id:
         type: string
         format: uuid
      additionalProperties: false
    
    CreateAppointmentDto:
     title: CreateAppointmentDto
     $ref: "#/components/schemas/Appointment/properties/appointmentData"


      
          
        

      