components:
  schemas:
    Appointment:
      title: Appointment
      allOf:
        - $ref: "#/components/schemas/AppointmentRequest"
        - type: object
          properties:
            id:
              type: string
              format: uuid
          additionalProperties: false

    AppointmentRequest:
      title: AppointmentRequest
      type: object
      properties:
        startTime:
          type: string
          format: date-time
        endTime:
          type: string
          format: date-time
        title:
          type: string
          nullable: false
      additionalProperties: false



      
          
        

    # --Error
    # --GuidValueResult
    # --AppointmentRequest
    # --Appointment
  # reponses: 
    # -- 400
    # -- 500
    # -- 409 conflict